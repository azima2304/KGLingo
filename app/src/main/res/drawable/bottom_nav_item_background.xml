<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Эффект нажатия -->
    <item android:drawable="@drawable/bottom_nav_item_selected" android:state_checked="true" />
    <!-- Эффект наведения -->
    <item android:drawable="@drawable/bottom_nav_item_hover" android:state_hovered="true" />
    <!-- Состояние по умолчанию -->
    <item android:drawable="@android:color/transparent"  />
</selector>
